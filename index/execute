Mysql select语句的执行顺序，按照一般逻辑是这样的
1 执行from 语句拿到这张表的地址指针，类似目录作为特殊文件的存在。
2 执行where语句，从这张表中，检索拿到符合条件的记录。(一般是通过索引)。
3 执行group语句，把拿到的记录进行分组。
4 执行order by语句，将分组/未分组的数据按照字段进行排序。
5 执行select语句，选择记录中需要的字段。最终从3306返回给客户端。

人才是培养出来的，不是挖来的。挖来的人才会服你吗。